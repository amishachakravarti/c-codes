#include<iostream>
using namespace std;
int main(){
    float a,b;
    cout<< "Enter a and b:= ";
    cin>>a>>b;
    if(a==b) cout<<"Both are equal";
    else{ if(a>b) cout<<"a is greater";
        else cout<<"a is smaller than b";}
        
    
}